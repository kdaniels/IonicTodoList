import { CustomCompletePipe } from './custom-complete.pipe';

describe('CustomCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
